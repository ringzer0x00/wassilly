module In = Input.SFLInput
module Out = Output.SFLOutput
module Sk = Stack.SFLStack
module Ca = Cache.CacheSFL
module SCG = Scg.SFLSCG
(*
module Interpreter (In) (Out) (Sk) (Ca) (SCG) :
  Genericcomponents.Interpreter.GI = struct
  (*
  let evalG (fix : I.t -> I.sk -> I.ca -> I.out * I.ca * I.scg) (in_ : I.in_) (sk : I.sk) : I.out * I.ca * I.scg =
    I.evalG

  let fix = I.fix
  let eval = evalG fix*)

end
*)