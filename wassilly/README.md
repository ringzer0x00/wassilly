# Usage

```
wassilly <file.wasm> [-v | -o <graph.dot> | -s <specfile.silly>]
  -v (verbose) Output debug information
  -s (spec) path to specification file
  -o (output) Set output file name
  -help  Display this list of options
  --help  Display this list of options
```